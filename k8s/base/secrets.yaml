# Secrets template - actual values should be applied separately via kubectl or external secrets
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: tequity
type: Opaque
stringData:
  # These are placeholders - replace with actual secrets
  # In production, use External Secrets Operator or Sealed Secrets
  DATABASE_URL: "placeholder"
  JWT_SECRET: "placeholder"
  ENCRYPTION_KEY: "placeholder"
  GCP_PROJECT_ID: "tequity-ajit"
  PULUMI_ACCESS_TOKEN: "placeholder"

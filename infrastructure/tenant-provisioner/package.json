{
  "name": "@tequity/tenant-provisioner",
  "version": "1.0.0",
  "private": true,
  "description": "Pulumi infrastructure for tenant provisioning (Cloud SQL + Storage per tenant)",
  "main": "index.ts",
  "scripts": {
    "build": "tsc",
    "preview": "pulumi preview",
    "up": "pulumi up",
    "destroy": "pulumi destroy",
    "stack:init:dev": "pulumi stack init dev",
    "stack:init:prod": "pulumi stack init prod"
  },
  "dependencies": {
    "@pulumi/pulumi": "^3.100.0",
    "@pulumi/gcp": "^7.10.0",
    "@pulumi/random": "^4.16.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}

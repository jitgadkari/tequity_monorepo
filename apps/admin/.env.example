# Database Configuration
DATABASE_URL=postgresql://user:password@host:port/database

# JWT Secret for Admin Authentication
# Generate a secure random string for production
JWT_SECRET=your-secret-key-change-in-production

# WeaveDesk Application URL
# URL where WeaveDesk (customer application) is hosted (for setup URLs)
# Development: http://localhost:3001
# Production: https://weavedesk.com
NEXT_PUBLIC_CUSTOMER_APP_URL=http://localhost:3001

# Service-to-Service Authentication
# API key for customer applications to authenticate with the admin platform
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SERVICE_API_KEY=generate-a-long-random-secure-key-here-at-least-32-chars
